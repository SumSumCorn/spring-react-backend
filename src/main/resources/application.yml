spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mg_db
    username: mg
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5Dialect
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true